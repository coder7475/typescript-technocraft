"use strict";
{
    // type assertion
    let anything;
    anything = "Apollo";
    anything.length;
    const kgToGM = (value) => {
        // 
    };
    try {
    }
    catch (error) {
        console.log(error.message);
    }
}
