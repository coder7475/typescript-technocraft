// interface similiar to type 
// usefull to create custom types for objects
type User1 = {
    name: string;
    age: number;
}

type roll = number;

interface User2 {
    name: string;
    age: number;
}