"use strict";
{
    const user1 = {
        name: "A",
        age: 4,
        role: "done",
    };
    const user2 = {
        name: "A",
        age: 4,
        role: "done",
    };
}
const roll1 = [1, 2, 3];
const roll2 = [1, 2, 3];
;
{
    const add = (x, y) => x + y;
    const sub = (x, y) => x + y;
}
